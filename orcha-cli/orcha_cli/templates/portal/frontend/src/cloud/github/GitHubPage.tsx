import { Shell } from "../../shell/Shell";

// Cloud page stub — replaced by the full port from the vanilla cloud page.
export function GitHubPage() {
  return (
    <Shell page="github" title="GitHub">
      <div className="muted" style={{ padding: 24 }}>GitHub — port in progress.</div>
    </Shell>
  );
}
