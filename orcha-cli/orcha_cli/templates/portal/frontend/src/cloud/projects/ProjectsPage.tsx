import { Shell } from "../../shell/Shell";

// Cloud page stub — replaced by the full port from the vanilla cloud page.
export function ProjectsPage() {
  return (
    <Shell page="projects" title="Projects">
      <div className="muted" style={{ padding: 24 }}>Projects — port in progress.</div>
    </Shell>
  );
}
