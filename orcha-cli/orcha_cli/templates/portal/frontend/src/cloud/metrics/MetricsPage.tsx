import { Shell } from "../../shell/Shell";

// Cloud page stub — replaced by the full port from the vanilla cloud page.
export function MetricsPage() {
  return (
    <Shell page="metrics" title="Metrics">
      <div className="muted" style={{ padding: 24 }}>Metrics — port in progress.</div>
    </Shell>
  );
}
