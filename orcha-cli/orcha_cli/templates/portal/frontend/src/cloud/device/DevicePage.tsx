import { Shell } from "../../shell/Shell";

// Cloud page stub — replaced by the full port from the vanilla cloud page.
export function DevicePage() {
  return (
    <Shell page="device" title="Device">
      <div className="muted" style={{ padding: 24 }}>Device — port in progress.</div>
    </Shell>
  );
}
