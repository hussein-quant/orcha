import { Shell } from "../../shell/Shell";

// Cloud page stub — replaced by the full port from the vanilla cloud page.
export function MembersPage() {
  return (
    <Shell page="members" title="Members">
      <div className="muted" style={{ padding: 24 }}>Members — port in progress.</div>
    </Shell>
  );
}
